{
  "name": "tidbyt-mealviewer",
  "version": "1.0.0",
  "scripts": {
    "start": "wrangler dev --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.60.3",
    "@cloudflare/puppeteer": "^0.0.14"
  }
}
